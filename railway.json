{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn app:create_app --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --forwarded-allow-ips='*'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120
  }
}
