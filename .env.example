# VitaLog Environment Variables — copy to .env for local dev
# Railway auto-injects DATABASE_URL via the PostgreSQL plugin.

SECRET_KEY="generate-a-strong-random-key-with-python-secrets"
DATABASE_URL="sqlite:///vitalog.db"
